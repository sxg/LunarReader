# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'LunarReader' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Pulley'
  pod 'Disk', '~> 0.6.2'
  pod 'ValueStepper'
  pod 'Fabric'
  pod 'Crashlytics'

  # Pods for LunarReader

  target 'LunarReaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LunarReaderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
